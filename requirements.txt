# Core orchestration + LLM
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
openai>=1.40.0

# Real web search for the research agent
tavily-python>=0.5.0

# Typed state + structured agent outputs
pydantic>=2.7.0

# Readability scoring for the SEO agent
textstat>=0.7.3

# Markdown -> semantic HTML for the Medium-import export
markdown>=3.6

# Visual demo UI
streamlit>=1.36.0

# Secrets loading
python-dotenv>=1.0.0

# Tests (mock mode, no API keys required)
pytest>=8.0.0

# ===================== V2 additions =====================
# FastAPI streaming backend
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0

# Persistent vector memory
chromadb>=0.5.0

# Full-page fetch for deep-research RAG (httpx is also a langchain dep)
beautifulsoup4>=4.12.0
httpx>=0.27.0
